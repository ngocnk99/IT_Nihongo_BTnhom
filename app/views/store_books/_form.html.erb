<%= form_with(model: store_book, local: true) do |form| %>
  <% if store_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(store_book.errors.count, "error") %> prohibited this store_book from being saved:</h2>

      <ul>
        <% store_book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :store_id %>
    <%= form.number_field :store_id %>
  </div>

  <div class="field">
    <%= form.label :book_id %>
    <%= form.number_field :book_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
